# 前额叶训练场 · 设计与交接文档

> 本文档是**本项目的产品与交接规格**。任何人接手开发，读完本文即可继续改代码。
> 游戏设计通论（MDA、心流、技能原子、有意思的决定）另见 [game-design.md](game-design.md)。
> 十关手感问题与逐关改进清单见 [improvements.md](improvements.md)。
> 最后更新：2026-08-28（舒尔特透明底动物剪影，目录自动读图）

## 1. 项目定位与理念

### 是什么

- 工作名：PrefrontalCortexGame（前额叶训练场）
- 类型：移动端认知训练小游戏合集
- 形态：一个 hub 首页 + 10 个独立小游戏，**用户自己选择玩什么**
- 引擎：Godot 4.7+，纯 GDScript，无外部依赖，竖屏 720×1280 基准

### 核心理念（不可妥协）

前三条管「这是什么产品」，第四条管「这关值不值得留」。

1. **每个游戏只练一种执行功能。** 前额叶管执行功能（自控、专注、计划、决策、抑制冲动）。每个游戏直接对应一个认知科学里真实存在的任务范式，练的就是那个范式练的东西，不混装。
2. **诚实标注。** 游戏只练「该能力在游戏内的熟练度」，不承诺迁移到现实、不承诺变聪明。神经可塑性需要数周数月；游戏提供的是「练习场」，不是「修复」。
3. **拒绝营销话术。** 文案中禁用：「3 分钟修复大脑」「激活 CEO」「多巴胺戒断」「变聪明」「左脑右脑开发」。每个游戏入口用一句平实的功能描述（如「抑制控制 · 干扰」）。
4. **要有挑战，能进心流。** 一直有事做、对错立刻知道、难度跟着水平涨、失败是你的失误不是运气。实验室范式成立不等于好玩：延迟折扣（棉花糖）空等加随机失败，已从清单拿掉。

两条打架时（范式成立但玩法是空等），**宁可换关，不要硬把流程搬上手机**。手感层面的模型、出处和十条清单见 [game-design.md](game-design.md)。

### 做一关的检查单

合入或改关前逐条过：

- 玩家随时知道现在要干什么
- 每一次操作立刻有反馈
- 没有大段空等
- 熟了以后题会变难，不是永远同一档
- 一种冲突就够，不要两套规则叠在一起
- 名字和说明只说这一关在练什么

### 认知科学依据（选型来源）

| 执行功能 | 有依据的范式 | 不采用的流行说法 |
| --- | --- | --- |
| 抑制控制 | Stroop、Go/No-Go、Stop-signal、Flanker | —— |
| 注意 | 舒尔特方格、多目标追踪（MOT） | 「专注力音乐」 |
| 工作记忆 | N-Back、瞬时计数（subitizing） | —— |
| 认知灵活性 | 任务切换范式 | —— |
| 计划 | 汉诺塔（Tower of Hanoi） | —— |

正念、睡眠、运动对前额叶最有效但属于生活干预，做不成街机机制，**不在游戏清单内**。

## 2. 架构

### 结构总览

```text
prefrontal-cortex-game/
├── project.godot     # main_scene = scenes/Hub.tscn；竖屏 720×1280
├── scenes/           # 每个场景只有根 Control + 脚本，节点树全部由代码构建
│   ├── Hub.tscn
│   ├── Stroop.tscn
│   ├── Schulte.tscn
│   ├── Dots.tscn
│   ├── GoNoGo.tscn
│   ├── StopSignal.tscn
│   ├── NBack.tscn
│   ├── Switch.tscn
│   ├── Track.tscn
│   ├── Flanker.tscn
│   └── Hanoi.tscn
├── scripts/
│   ├── GameBase.gd   # 所有游戏继承的脚手架
│   ├── Hub.gd        # GAMES 数组 = 游戏注册表
│   └── （每个游戏一个脚本）
└── docs/design.md    # 本文档
```

### 架构决策

- **场景文件只是壳。** .tscn 里只有 `Control` 根节点 + 脚本引用；所有 UI 节点在 `_ready()` 里用代码构建。这延续了同仓库 DodgeShotLab 的做法，避免编辑器二进制状态和代码漂移，diff 干净。
- **GameBase 提供公共外壳。** 深色背景（`#0d1220`）、左上角「< 返回」、右上角「?」玩法、居中标题。子类在 `_init()` 里设 `title_text` 和 `help_text`，并重写 `_build_game()`。打开玩法时暂停游戏树，避免计时在说明底下继续跑。答对/答错用 `_burst_feedback` 打出全屏大字 + 色闪，避免只靠一行小提示。记住关卡的游戏用同一套进度门：`_show_progress_gate` / `_on_progress_continue` / `_on_progress_fresh`（绿钮进最新一关，灰钮从头开始）。汉诺塔、追踪异色球、舒尔特方格已接；反向色字复用同一套 UI 选简单/困难。新关照抄，不要另做一套选择 UI。改某一关后对照 [improvements.md](improvements.md) 该关一节更新现状和勾选。
- **Hub 用数据驱动注册。** `GAMES` 数组是唯一注册点：`{title, func, group, scene, built}`。列表按 `group`（执行功能）分段；卡片两行（游戏名 + 练什么）。未实现的游戏 `built: false` → 置灰并在右侧标「即将推出」，功能说明仍可见；实现后翻成 `true` 并填 `scene` 路径即可。
- **游戏间零耦合。** 每个游戏一个场景 + 一个脚本，返回即 `change_scene_to_file` 回 hub，无共享运行时状态。本地成绩各自存 `user://*.cfg`。
- **Hub 检查 GitHub Release。** 进首页时请求 `releases/latest`，远程 tag 比 `application/config/version` 新则底部提示「有新版本」；点开浏览器下载页。失败静默；点 × 记下该 tag，不再提示。不自动安装。

### 代码规范（沿用本仓库惯例）

- GDScript，`class_name` 不使用，场景脚本用 `extends` + 文件名即类
- 每个函数前一行 `##` 文档注释（DodgeShotLab 风格）
- 常量全大写、`const`，可调数值集中在脚本顶部
- 颜色统一十六进制 `Color("#rrggbb")`；共享调色板见 §6
- 中文界面文案（按钮、状态、标题），代码注释可中可英
- UI 节点代码构建时用 `set_anchors_and_offsets_preset` 做锚点布局，位置类用固定像素（竖屏基准 720 宽）

## 3. 如何新增一个游戏（五步）

以「N-Back」为例：

1. **写脚本** `scripts/NBack.gd`：

```gdscript
extends "res://scripts/GameBase.gd"
## N-Back：判断当前刺激是否与 N 步前相同。


const LIVES := 3

var score := 0

func _init() -> void:
    title_text = "N-Back"
    help_text = "判断当前刺激是否与 N 步前相同。"

func _build_game() -> void:
    # 构建 HUD、主内容、按钮，并开始第一试次
    pass
```

2. **建场景** `scenes/NBack.tscn`（内容三行，照抄改 id 和脚本路径）：

```text
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/NBack.gd" id="1_nback"]

[node name="NBack" type="Control"]
script = ExtResource("1_nback")
```

3. **注册进 hub**：`scripts/Hub.gd` 的 `GAMES` 数组里找到对应条目，填 `scene` 路径、`built: true`。
4. **headless 验证**：

```bash
GODOT="/Applications/Godot.app/Contents/MacOS/Godot"
PROJ="<项目路径>"
"$GODOT" --headless --path "$PROJ" res://scenes/NBack.tscn --quit-after 5 2>&1 | grep -iE "error|invalid|failed|nonexistent|cannot|parse"
```

   无输出 = 通过。（经验：`add_theme_font_color_override` 不是真实 API，正确的是 `add_theme_color_override`。）
5. **肉眼验证 UI**：编辑器 F5，进游戏点几轮，确认布局、反馈、返回。对照 §1「做一关的检查单」（空等、反馈、难度会涨）。

## 4. 游戏总表

| # | 游戏 | 执行功能 | 范式 | 场景 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | 反向色字 | 抑制控制·干扰 | Stroop | Stroop.tscn | ✅ 已实现 |
| 2 | 反向反应 | 抑制控制·克制 | Go/No-Go | GoNoGo.tscn | ✅ 已实现 |
| 3 | 红灯停 | 抑制控制·刹车 | Stop-signal | StopSignal.tscn | ✅ 已实现 |
| 4 | 中间箭头 | 抑制控制·侧翼 | Flanker | Flanker.tscn | ✅ 已实现 |
| 5 | 舒尔特方格 | 专注·视觉搜索 | Schulte grid | Schulte.tscn | ✅ 已实现 |
| 6 | 追踪异色球 | 注意·追踪 | Multiple Object Tracking | Track.tscn | ✅ 已实现 |
| 7 | N-Back | 工作记忆·更新 | N-Back | NBack.tscn | ✅ 已实现 |
| 8 | 看点数 | 工作记忆·瞬时 | Subitizing/Iconic | Dots.tscn | ✅ 已实现 |
| 9 | 任务切换 | 认知灵活性 | Task-switching | Switch.tscn | ✅ 已实现 |
| 10 | 汉诺塔 | 计划·前瞻 | Tower of Hanoi | Hanoi.tscn | ✅ 已实现 |

十个游戏均已实现。

## 5. 各游戏规格

### 5.1 反向色字 Stroop ✅ 已实现

**玩法**：中央是有颜色的字，字义和字体颜色一定不同。点选项上**写着字体颜色名**的按钮。进场用进度门选简单或困难。

**已实现规格**（`scripts/Stroop.gd`）：

- 颜色集合：红 / 蓝 / 绿 / 黄。题目：放大的彩色字，字义与字体颜色始终不同。要点的是字体颜色对应的名字
- 进场用 GameBase 进度门：绿钮简单、灰钮困难。局内重开保持当前难度
- 简单：选项灰底黑字，每题只重排位置；困难：黑色颜色名 + 与字义不同的底色（干扰），字和底色都重排
- 每答完一题，四个选项位置都会变
- 每题固定 2.5 秒，倒计时条剩余 <25% 变红；超时视同错误；点选项会回弹，不停留在按下发灰态
- 试次间隔 0.35s：答对/点错/超时后锁输入，避免连点吃掉下一题；超时不会在同一帧连扣生命
- 常显提示「点选项上的字，对上题目字的颜色」；答对显示加分，点错/超时分别提示
- 计分：答对 10 分 + 剩余秒数×5 速度分 + 连击加成（每连击 +2，封顶 +20）。错题只扣命、不扣分
- 生命 3 次，错误/超时扣 1 并断连击；归零结束，可重开；本地存 `user://stroop_best.cfg`
- 参数集中在脚本顶部 `const`，可直接调

**已知的后续调优点**：速度分是否诱导乱点，仍待实玩验证（见 §8）。

### 5.2 舒尔特方格 Schulte ✅ 已实现

**玩法**：网格随机排数字，从 1 起按顺序点，越快越好。通关后格子变多。

**已实现规格**（`scripts/Schulte.gd`）：

- 档位 5×5 → 6×6 → 7×7（1–25 / 1–36 / 1–49）。点完当前档约 0.7s 后直接上更大的盘，格子中途不换位
- 每盘从 `assets/schulte/` 里的 PNG 抽一张动物**剪影**（实心外形，外面透明，无五官花纹）。运行时扫描该目录全部 `.png`，不用改脚本；中文名写在同目录 `names.cfg`，没有条目就用文件名。剖成与档位相同数量的不规则块（5×5=25、6×6=36、7×7=49）；数字叠在块上。点对块变亮、数字留下改成深色；点完露出整只动物再换图。档位仍是 5×5→6×6→7×7
- 首次点击开始本档计时；点错计数 + 红色闪烁（不中断流程）
- HUD 常显「动物  N×N  下一个 k／总数」；点错过才附加失误次数；每档单独计时、单独记最佳
- 格子边长按窗口可用空间计算（约 36–120px），重开按钮跟在网格下方；窗口拉伸会重排
- 本地记住当前档；再进时用 GameBase 进度门选「进入最新一关」或「从头开始」（5×5）；本地存 `user://schulte_best.cfg`（兼容旧的 5×5 `best.time`）
- 「重新开始」重排当前档；若刚好点完、下一档还没出来，则直接进下一档

### 5.3 看点数 Dots ✅ 已实现

**玩法**：屏幕短时间闪现随机分布的若干圆点，消失后用数字键盘输入看到的点数。

**已实现规格**（`scripts/Dots.gd`）：

- 一局 60 秒街机倒计时（开局 1 秒「准备」不扣时间），时间到结束；本地存最佳分 `user://dots_best.cfg`
- 点数 4–12 起步；答对先把闪现时间从 0.6s 每次 −0.02s（下限 0.35s），触底后点数上限 +1，不封顶；答错反向降难度
- 计分：正好 10 分 + 连击加成（每连击 +2，封顶 +20）；误差 ±1 给 5 分（半分，不升难度、不断连击）；误差 ≥2 断连击并降难度
- 点位用 Bridson 泊松盘铺满卡片，点间距约 3.8 倍半径；圆心限制在圆角+描边以内，整颗圆不出框；半径 9–14px
- UI：圆角卡片 + 闪现进度条；底部 0–9 + 删除 + 确定，非输入阶段键盘变淡；桌面可用键盘数字/退格/回车；面板和键盘随窗口重排
- 对完回显 1.8 秒（点改成对/错色，中间写出实际数量），再清空 0.5 秒显示「下一题」，对答案阶段不扣 60 秒

### 5.4 反向反应 GoNoGo ✅ 已实现

**玩法**：屏幕陆续出现彩色圆。默认「绿圆→点它，红圆→别点」。连续答对 10 次后规则反转（红点绿停），顶部色块同步改动词。

**已实现规格**（`scripts/GoNoGo.gd`）：

- 刺激最多显示 1.0s，未反应即过；试次间隔 0.9–1.4s 随机；开局 1 秒准备
- Go 试次 75%；连续 4 个 Go 后强制一个 No-Go，避免一直连点
- 计分：Go 命中 +10；No-Go 忍住 +15；误点 No-Go 或 Go 没点到都扣 1 命并断连击
- 生命 3 次，归零结束；本地存 `user://gonogo_best.cfg`
- 刺激弹出时圆和底部「点」按钮微微放大；非刺激阶段按钮变淡；桌面可用空格/回车
- 顶部常显规则色块：绿/红底上写「点」或「停」

**已知的后续调优点**：间隔 0.9–1.4s 偏稳，若觉得「痒」不够可把 `GAP_MIN/MAX` 下调。

### 5.5 红灯停 StopSignal ✅ 已实现

**玩法**：小车从左开向终点线，**靠近绿线之后**才能冲。约 1/4 的轮次在 40%–70% 进度处突然亮红灯，亮了就必须停手。

**已实现规格**（`scripts/StopSignal.gd`）：

- 行驶时长 1.2–2.0s 随机；Stop 试次 50%；连续 2 个绿灯后强制红灯，且不连续出两次红灯（大约隔一轮就有灯）
- 绿灯只有进度 ≥72%（绿线）才算冲过；提前点提示「还没到」，不给分、不扣命
- 红灯出现在 40%–70%，一定在冲刺窗口之前；亮后忍住 0.5s 即算刹住；红灯期间点了扣命
- 计分：绿灯冲过 +10；红灯刹住 +20；绿灯没冲或红灯没刹都扣 1 命
- 生命 3 次；开局 1 秒准备；试次间隔 0.55–0.9s；本地存 `user://stopsignal_best.cfg`
- 赛道有绿线冲刺区 + 黄线终点；靠近绿线时「冲」按钮脉动加强

**已知的后续调优点**：红灯约一半，若仍觉得稀或太密，调 `STOP_RATIO` / `MAX_GO_STREAK`。

### 5.6 N-Back ✅ 已实现

**玩法**：3×3 宫格依次亮一个位置，判断是否与 N 步前相同。点「相同」或「不同」。

**已实现规格**（`scripts/NBack.gd`）：

- N=2 起步，封顶 4，下限 2；刺激间隔 2.5s（亮 0.9s，其余作答窗口）
- 开始前 N 步只记位置、不判分；之后答对 +10，答错或超时扣 1 命
- 「相同」试次约 30%；连对 10 升 N，连错 3 降 N
- HUD 常显「N-Back · 往前第 N 个」；开局先说明要记住往前第几格，点「开始」后才出题；生命 3 次；本地存 `user://nback_best.cfg`
- 桌面可用 F/左 = 相同，J/右 = 不同

**已知的后续调优点**：刺激通道仍只有位置，颜色/声音混合（Dual N-Back）留到后续版本。

### 5.7 任务切换 Switch ✅ 已实现

**玩法**：中央数字 1–9（无 5），上方色块是规则：蓝=奇偶，橙=大于/小于 5。

**已实现规格**（`scripts/Switch.gd`）：规则每 2–4 试次切换；重复答对 +10，切换答对 +20；每题 3 秒；生命 3；本地最佳分。

### 5.8 追踪异色球 Track ✅ 已实现

**玩法**：黄球高亮 1.5s，全部变蓝后乱跑，停下点出原来的异色球。

**已实现规格**（`scripts/Track.gd`）：6–12 球，异色 1–3；移动时间 = `(4+0.5×(球数−6)) × ⌈(15−球数)/2⌉`（约 13–20 秒，球少更久）；移速 270–420 像素/秒；初始方向避开横竖轴各 20°；边界反弹无球间碰撞；全对 +20 并加球；错/漏扣命；本地记住当前球数，再进时选「进入最新一关」或「从头开始」（6 个球），进度门与汉诺塔同一套 UI；本地最佳分。

### 5.9 中间箭头 Flanker ✅ 已实现

**玩法**：五个箭头一排，只点最中间那个的方向。两侧常常指向反方向。

**已实现规格**（`scripts/Flanker.gd`）：Eriksen Flanker；一致 / 不一致试次；开局每题 1.15 秒，连对每 3 次 −0.05 秒，下限 0.52 秒；点错略放宽时限；不一致比例随加速从 55% 升到 82%；间隔 0.22 秒；生命 3 次；本地最佳分。延迟折扣（棉花糖）已从清单拿掉：空等加随机失败进不了心流。

### 5.10 汉诺塔 Hanoi ✅ 已实现

**玩法**：三柱移盘，点来源再点目标；大不能压小。

**已实现规格**（`scripts/Hanoi.gd`）：3 盘起，最优通关 +1 盘封顶 6；基础分 N×30，最优再 +N×50；非最优仍可通过；用时只展示；底部「复位」把盘子收回左柱并清零本关步数（总分保留）；本地记住当前盘数，再进时用 GameBase 进度门选「进入最新一关」或「从头开始」（3 盘）；本地最佳分。

## 6. 共享视觉规范

- 背景 `#0d1220`；卡片/按钮底 `#1a2740`；主文字 `#e8edff`；次文字 `#7f8ba6`
- 功能色：错误/危险 `#ff5d73`，成功 `#4ade80`，强调 `#ffd75d`，信息 `#8ab4ff`
- 四色游戏色板（Stroop 等）：红 `#ff5d73` / 蓝 `#5d9bff` / 绿 `#4ade80` / 黄 `#ffd75d`
- 按钮：圆角 10–14，hover 提亮 12%，按下压暗 15%（`GameBase._style_button` 已实现，可直接调用）
- 大字号适配移动端：正文 ≥18，主内容 ≥30，标题 32–36

## 7. 验证流程（每个游戏合入前）

1. headless 跑场景无报错（命令见 §3 第 4 步）
2. F5 实跑：完成至少一整局、故意输一局、返回 hub 再进（验证场景切换无残留）
3. 检查 hub 按钮状态与 design.md 总表状态同步
4. 对照 §1 检查单：无大段空等、对错立刻反馈、难度会涨、失败不是纯运气

发布说明只写 **功能点** 和 **优化点**，不写打包过程、签名、安装步骤。Android APK 作为附件挂上即可。

## 8. 首版待验证问题（产品层面）

- Stroop 困难模式：黑字 + 底色干扰，选项字对题目字体颜色；简单模式灰底黑字。实玩看困难是否又变成对色、简单是否太易。速度分是否诱导乱点仍待验证（若是：错误惩罚改为 −10 分）
- 10 个入口玩家是否看得懂各练什么（hub 已分组；局内右上角「?」可看玩法）
- 合集的整体留存动力：纯自选是否足够，还是需要「每日一练」轻结构（留到 10 个全齐后再评估）

## 9. 明确不做（当前版本）

- 账号系统、云同步、排行榜对战
- 正念/呼吸类内容（生活干预，不是街机机制）
- 空等型关卡（点一下然后盯着计时/随机成败；延迟折扣已因此换掉）
- 「脑年龄」「智商分」这类量化包装（违反诚实原则）
- 提醒通知、打卡系统（先验证游戏本身是否好玩）
- iOS / iPhone 安装包（发布只打 Android APK）
